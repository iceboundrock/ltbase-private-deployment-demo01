{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Log",
  "description": "Activity log record for lead visits and notes",
  "type": "object",
  "required": [
    "id",
    "ownerId",
    "type",
    "createdAt"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Log identifier",
      "pattern": "^[a-zA-Z0-9_-]+$",
      "format": "uuid"
    },
    "leadId": {
      "$ref": "lead.json#/$defs/lead_id"
    },
    "visitId": {
      "$ref": "visit.json#/$defs/visit_id"
    },
    "noteId": {
      "type": "string",
      "description": "Associated note ID (if log originated from a note)",
      "x-ltbase-note-prop": "${note_id}"
    },
    "data": {
      "type": "string",
      "x-ltbase-note-prop": "${note_data}",
      "x-ltbase-type": "virtual"
    },
    "ownerId": {
      "type": "string",
      "description": "Owner/creator of this log entry",
      "x-ltbase-note-prop": "${owner_id}"
    },
    "summary": {
      "type": "string",
      "description": "Summary or transcription of the log content"
    },
    "type": {
      "type": "string",
      "description": "MIME type of the log data",
      "enum": [
        "audio/mp3",
        "audio/wav",
        "audio/webm",
        "image/jpeg",
        "image/png",
        "image/webp",
        "text/plain"
      ]
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "Record creation timestamp"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Last update timestamp"
    },
    "createdBy": {
      "type": "string",
      "description": "User ID of who created the visit record"
    },
    "updatedBy": {
      "type": "string",
      "description": "User ID of who last updated the visit record"
    },
    "deletedBy": {
      "type": "string",
      "description": "User ID of who deleted the visit record"
    },
    "ownerTeam": {
      "type": "string",
      "description": "Team that owns the visit record"
    }
  }
}
